[package]
name = "kitmux-model"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
publish = false

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
uuid = { version = "=1.23.4", features = ["serde", "v4"] }

[dev-dependencies]
serde_json = "=1.0.150"
