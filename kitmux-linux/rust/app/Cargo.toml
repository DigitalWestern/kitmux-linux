[package]
name = "kitmux"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
publish = false

[dependencies]
gtk = { package = "gtk4", version = "=0.11.4", features = ["v4_22"] }
kitmux-model = { path = "../model" }
libc = "=0.2.189"
